import React from "react";

const Button = () => {
  return (
    <React.Fragment>
      <button type="submit" className="btn__overlay--full">
        Proceed to Payment
      </button>
    </React.Fragment>
  );
};

export default Button;
